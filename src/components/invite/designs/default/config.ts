/** Hardcoded configuration for the default invitation design. */
export const defaultConfig = {
  showCountdown:    true,
  countdownHeading: 'Count Down',
  showSchedule:     true,
  showRsvp:         true,
  rsvpMessage:      'In case of any query, feel free to reach out to us.',
} as const;
